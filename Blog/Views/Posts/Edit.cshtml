@model Blog.Data.Models.Post
@{
    ViewBag.Title = "Редагувати пост";
}

@Styles.Render("~/public/css/post.css")
@Styles.Render("~/public/css/post_responsive.css")

<div class="container">
    <div class="row" data-ref="container">
        <div class="col-lg-12">
            <h2 class="text-center" id="form-header">Написати пост</h2>
            @using (Html.BeginForm("Edit", "Posts", FormMethod.Post, new { role = "form" }))
            {
                @Html.AntiForgeryToken()
                @Html.ValidationSummary("", new { @class = "text-danger" })
                <div class="form-group">
                    <label
                        tag="">
                        Тема посту:
                    </label>

                    @Html.TextBoxFor(model => model.Title, new { @class = "form-control", @id = "title", @placeholder = "Тема посту" })
                </div>

                <div class="form-group">
                    <label
                        tag="">
                        Короткий опис:
                    </label>

                    @Html.TextAreaFor(model => model.Description, new { @class = "form-control", @id = "author", @placeholder = "Опис" })
                </div>

                <div class="form-group">
                    <label
                        tag="">
                        Пост:
                    </label>

                    @Html.TextAreaFor(model => model.Content, new { @class = "comment_text", @id = "content", @placeholder = "Текст посту" })
                </div>

                <label
                    tag="">
                    Основна картинка
                </label>
                <!--
                    <div class="radio">
                        <label>
                            <input class="form-check-input" type="radio" name="type" id="upload" value="upload">
                            <label for="upload">Вибрати файл з комп'ютера</label>
                        </label>
                        <div class="upload" style="display: none;">
                            <div id="objects">
                                <div class="form-group" id="add-image1" >
                                    <input type="hidden" name="MAX_FILE_SIZE" value="2000000" />
                                    <label for="gallery_img" tag="" class="optional">Картинка:</label>
                                    <dd>
                                        <input type="hidden" name="MAX_FILE_SIZE" value="2097152" id="MAX_FILE_SIZE">
                                        <input type="file" name="gallery_img" id="gallery_img" class="form-control">
                                    </dd>
                                </div>
                            </div>
                            <p id="add-input" class="btn btn-default">Додати ще одну картинку</p>
                        </div>
                    </div>
                    <div class="radio">
                        <label>
                            <input class="form-check-input" type="radio" name="type" id="addurl" value="addurl">
                            <label for="addurl">Вказати URL-адресу</label>
                        </label>
                        <div class="addurl" style="display: none;">
                            <div class="form-group">
                                <label for="img_url" tag="" class="optional">URL-адреса:</label>
                                <input type="text" name="img_url" id="img_url" value="" class="form-control">
                                <p>Наприклад: https://some_image.com/example.jpg</p>
                            </div>
                        </div>
                    </div>
                -->
                <div>
                    <div class="form-group">
                        <label
                            tag="">
                            URL-адреса:
                        </label>

                        @Html.TextBoxFor(model => model.ImageUrl, new { @class = "form-control", @id = "title", @placeholder = "URL-адреса" })

                        <p>Наприклад: https://some_image.com/example.jpg</p>
                    </div>
                </div>

                @Html.TextBoxFor(model => model.Tags, new { @class = "form-control", @id = "tags", @placeholder = "Теги" })


                <!--
                <div class="form-group">
                    <div class="panel panel-primary">
                        <div class="panel-heading">
                            <span class="fa fa-list"></span> Теги:
                        </div>
                        <div class="panel-body">
                            <ul class="list-group">

                                <li class="list-group-item" *ngFor="let tag of _tags">
                                    <div class="checkbox">
                                        <label>
                                            { {tag} }
                                        </label>
                                    </div>
                                    <div class="pull-right action-buttons">
                                        <button type="button" (click)="_editTag(tag)"><span class="fa fa-pencil"></span></button>
                                        <button type="button" (click)="_onDeleteTagAction(tag)"><span class="fa fa-trash"></span></button>
                                    </div>
                                </li>

                            </ul>
                        </div>
                    </div>

                    <label for="alias"
                           tag=""
                           class="optional">
                        { {_tagLabel} }:
                    </label>

                    <input type="text"
                           name="tags"
                           id="tags"
                           #tag
                           value="{ {_selectedTag.value} }"
                           class="form-control">

                    <input type="button"
                           name="tagAction"
                           id="tagAction"
                           value="{ {_tagLabel} }"
                           class="btn btn-default"
                           (click)="_tagAction(tag.value, _action)">
                </div>

                <div class="form-group">
                    <label for="alias"
                           tag=""
                           class="optional">
                        Тема посту на англійській і без пропусків(посилання):
                    </label>
                    <input type="text"
                           name="alias"
                           id="alias"
                           value=""
                           class="form-control">
                </div>
                -->
                <br />
                <div class="pull-right">
                    <input type="submit"
                           name="submit"
                           id="submit"
                           value="Редагувати"
                           class="btn btn-default">

                    <button type="button"
                            class="btn btn-sm text-white btn-secondary">
                        Скасувати
                    </button>
                </div>
            }
        </div>
    </div>
</div>
@Scripts.Render("~/public/plugins/jquery/jquery.js")
@Scripts.Render("~/public/plugins/tinymce/tinymce.min.js")
@Scripts.Render("~/public/js/form.js")